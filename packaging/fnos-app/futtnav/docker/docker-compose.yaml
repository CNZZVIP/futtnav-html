services:
  futtnav:
    image: ${DOCKER_MIRROR}docker.io/nginxinc/nginx-unprivileged:${VERSION}
    container_name: futtnav
    ports:
      - "${TRIM_SERVICE_PORT}:8080"
    volumes:
      - ${TRIM_PKGVAR:?}/www:/usr/share/nginx/html:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
